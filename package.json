{
  "private": true,
  "name": "@enghouse-qumu/widget",
  "version": "1.0.0-beta.7",
  "description": "Qumu Cloud widget library",
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/qumu/qc-widget.git"
  },
  "bugs": {
    "url": "https://github.com/qumu/qc-widget/issues"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:watch": "npm run dev:watch --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "server": "npm run server -w lib"
  },
  "workspaces": [
    "packages/*"
  ],
  "volta": {
    "node": "24.11.0"
  }
}
